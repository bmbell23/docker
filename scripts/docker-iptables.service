[Unit]
Description=Fix Docker iptables rules for all containers
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStartPre=/bin/sleep 30
ExecStart=/home/brandon/projects/docker/scripts/fix-all-docker-iptables.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target

